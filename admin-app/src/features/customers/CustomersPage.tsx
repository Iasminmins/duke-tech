import { FormEvent, useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { createCustomer, listCustomers } from './customerRepository'; import { customerSchema, type CustomerInput } from './customerSchema';
type Customer=CustomerInput&{id:string;created_at:string}; const blank:CustomerInput={full_name:'',phone:'',whatsapp:'',email:'',cpf:'',address:'',notes:''};
export function CustomersPage(){const [rows,setRows]=useState<Customer[]>([]);const [search,setSearch]=useState('');const [form,setForm]=useState<CustomerInput>(blank);const [open,setOpen]=useState(false);const [error,setError]=useState('');const [success,setSuccess]=useState('');const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);
  function load(){setLoading(true);listCustomers(search).then(({data,error})=>{if(error)setError('Não foi possível carregar os clientes.');else if(data)setRows(data as Customer[]);setLoading(false)})}
  useEffect(()=>{const timer=window.setTimeout(load,250);return ()=>window.clearTimeout(timer)},[search]);
  async function submit(e:FormEvent){e.preventDefault();if(saving)return;const parsed=customerSchema.safeParse(form);if(!parsed.success){setError(parsed.error.issues[0]?.message||'Revise os campos.');return}setSaving(true);setError('');const result=await createCustomer(parsed.data);setSaving(false);if(result.error){setError(result.error.message);return}setForm(blank);setOpen(false);setError('');setSuccess('Cliente cadastrado com sucesso.');setRows(prev=>result.data?[result.data as Customer,...prev]:prev)}
  return <section className="content"><div className="page-toolbar-top"><span className="page-toolbar-count">{!loading ? `${rows.length} ${rows.length === 1 ? 'cliente' : 'clientes'}` : ''}</span><button className="btn primary" onClick={()=>{setOpen(!open);setError('');setSuccess('')}}>+ Novo cliente</button></div>{success&&<div className="success" role="status">{success}</div>}{open&&<form className="form-card" onSubmit={submit} noValidate><div className="form-grid">{[['full_name','Nome completo'],['phone','Telefone'],['whatsapp','WhatsApp'],['email','E-mail'],['cpf','CPF/CNPJ (opcional)'],['address','Endereço']].map(([key,label])=><div className="field" key={key}><label htmlFor={`customer-${key}`}>{label}</label><input id={`customer-${key}`} className="input" value={form[key as keyof CustomerInput]||''} onChange={e=>setForm({...form,[key]:e.target.value})} disabled={saving}/></div>)}<div className="field full"><label htmlFor="customer-notes">Observações</label><textarea id="customer-notes" className="textarea" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})} disabled={saving}/></div></div>{error&&<div className="error" role="alert" style={{marginTop:16}}>{error}</div>}<div className="actions"><button type="button" className="btn" onClick={()=>setOpen(false)} disabled={saving}>Cancelar</button><button className="btn primary" disabled={saving} aria-busy={saving}>{saving?'Salvando…':'Salvar cliente'}</button></div></form>}<div className="toolbar"><input className="input search" placeholder="Buscar por nome ou telefone" aria-label="Buscar cliente" value={search} onChange={e=>setSearch(e.target.value)}/></div>{error&&!open&&<div className="error" role="alert" style={{marginBottom:16}}>{error} <button className="btn" onClick={load} style={{marginLeft:8}}>Tentar novamente</button></div>}<div className="table-wrap"><table className="table"><thead><tr><th>Cliente</th><th>Telefone</th><th>Cadastro</th><th></th></tr></thead><tbody>{loading?<tr><td colSpan={4} className="empty">Carregando clientes…</td></tr>:rows.length===0?<tr><td colSpan={4} className="empty">{search?'Nenhum cliente encontrado para esta busca.':'Nenhum cliente cadastrado.'}</td></tr>:rows.map(row=><tr key={row.id}><td><strong>{row.full_name}</strong><br/><span className="muted">{row.email||'Sem e-mail'}</span></td><td>{row.phone}</td><td>{new Date(row.created_at).toLocaleDateString('pt-BR')}</td><td><Link to={`/admin/clientes/${row.id}`} className="btn">Detalhes</Link></td></tr>)}</tbody></table></div></section>}
