import { FormEvent, useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { createCustomer, listCustomers } from './customerRepository'; import { customerSchema, type CustomerInput } from './customerSchema';
type Customer=CustomerInput&{id:string;created_at:string}; const blank:CustomerInput={full_name:'',phone:'',whatsapp:'',email:'',cpf:'',address:'',notes:''};
export function CustomersPage(){const [rows,setRows]=useState<Customer[]>([]);const [search,setSearch]=useState('');const [form,setForm]=useState<CustomerInput>(blank);const [open,setOpen]=useState(false);const [error,setError]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);listCustomers(search).then(({data,error})=>{if(!error&&data)setRows(data as Customer[]);setLoading(false)})},[search]);async function submit(e:FormEvent){e.preventDefault();const parsed=customerSchema.safeParse(form);if(!parsed.success){setError(parsed.error.issues[0]?.message||'Revise os campos.');return}const result=await createCustomer(parsed.data);if(result.error){setError(result.error.message);return}setForm(blank);setOpen(false);setError('');setRows(prev=>result.data?[result.data as Customer,...prev]:prev)}return <section className="content"><div className="page-heading"><div><div className="eyebrow">Relacionamento</div><h2>Clientes</h2><p>Cadastro e histórico dos clientes da loja.</p></div><button className="btn primary" onClick={()=>setOpen(!open)}>+ Novo cliente</button></div>{open&&<form className="form-card" onSubmit={submit}><div className="form-grid">{[['full_name','Nome completo'],['phone','Telefone'],['whatsapp','WhatsApp'],['email','E-mail'],['cpf','CPF (opcional)'],['address','Endereço']].map(([key,label])=><div className="field" key={key}><label>{label}</label><input className="input" value={form[key as keyof CustomerInput]||''} onChange={e=>setForm({...form,[key]:e.target.value})}/></div>)}<div className="field full"><label>Observações</label><textarea className="textarea" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></div></div>{error&&<div className="error" style={{marginTop:16}}>{error}</div>}<div className="actions"><button type="button" className="btn" onClick={()=>setOpen(false)}>Cancelar</button><button className="btn primary">Salvar cliente</button></div></form>}<div className="toolbar"><input className="input search" placeholder="Buscar por nome" value={search} onChange={e=>setSearch(e.target.value)}/></div><div className="table-wrap"><table className="table"><thead><tr><th>Cliente</th><th>Telefone</th><th>Cadastro</th><th></th></tr></thead><tbody>{loading?<tr><td colSpan={4} className="empty">Carregando clientes…</td></tr>:rows.length===0?<tr><td colSpan={4} className="empty">Nenhum cliente cadastrado.</td></tr>:rows.map(row=><tr key={row.id}><td><strong>{row.full_name}</strong><br/><span className="muted">{row.email||'Sem e-mail'}</span></td><td>{row.phone}</td><td>{new Date(row.created_at).toLocaleDateString('pt-BR')}</td><td><Link to={`/admin/clientes/${row.id}`} className="btn">Detalhes</Link></td></tr>)}</tbody></table></div></section>}
