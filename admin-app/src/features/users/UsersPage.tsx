import { FormEvent, useEffect, useState } from 'react';
import { supabase } from '../../lib/supabase';
import { canDeactivateProfile, canManageUsers, permissionsByRole, roleLabels, type Role } from './userUtils';
type Profile = { id: string; full_name: string; role: Role; active: boolean; created_at: string };
export function UsersPage() { const [rows,setRows]=useState<Profile[]>([]); const [name,setName]=useState(''); const [role,setRole]=useState<Role>('employee'); const [open,setOpen]=useState(false); const [loading,setLoading]=useState(true); const [error,setError]=useState(''); const [success,setSuccess]=useState(''); async function load(){if(!supabase){setLoading(false);return} const result=await supabase.from('profiles').select('id,full_name,role,active,created_at').order('created_at'); if(result.error)setError('Não foi possível carregar a equipe.'); setRows((result.data||[]) as Profile[]);setLoading(false)} useEffect(()=>{void load()},[]); async function submit(e:FormEvent){e.preventDefault();if(!supabase||!name.trim()){setError('Informe o nome do membro.');return}setError('');const result=await supabase.from('profiles').insert({full_name:name.trim(),role,active:true}).select('id,full_name,role,active,created_at').single();if(result.error||!result.data){setError('O perfil precisa estar vinculado a um usuário autenticado antes do cadastro.');return}setRows(prev=>[...prev,result.data as Profile]);setName('');setRole('employee');setOpen(false);setSuccess('Perfil salvo.')} async function toggle(row:Profile){if(!supabase)return;const admins=rows.filter(item=>item.active&&item.role==='admin').length;if(row.active&&!canDeactivateProfile({actorRole:'admin',targetRole:row.role,activeAdmins:admins})){setError('O último administrador não pode ser desativado.');return}const result=await supabase.from('profiles').update({active:!row.active}).eq('id',row.id);if(result.error)setError('Não foi possível atualizar o acesso.');else setRows(prev=>prev.map(item=>item.id===row.id?{...item,active:!item.active}:item))}return <section className="content"><div className="page-toolbar-top"><span className="page-toolbar-count">{!loading ? `${rows.length} ${rows.length === 1 ? 'membro' : 'membros'}` : ''}</span>{canManageUsers('admin')&&<button className="btn primary" onClick={()=>setOpen(!open)}>+ Adicionar membro</button>}</div>{success&&<div className="success" role="status">{success}</div>}{error&&<div className="error" role="alert">{error}</div>}{open&&<form className="form-card" onSubmit={submit}><h3>Novo perfil</h3><div className="form-grid"><div className="field"><label htmlFor="user-name">Nome completo</label><input id="user-name" className="input" value={name} onChange={e=>setName(e.target.value)} required /></div><div className="field"><label htmlFor="user-role">Papel</label><select id="user-role" className="select" value={role} onChange={e=>setRole(e.target.value as Role)}>{Object.entries(roleLabels).map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></div></div><div className="actions"><button type="button" className="btn" onClick={()=>setOpen(false)}>Cancelar</button><button className="btn primary">Salvar perfil</button></div></form>}<section className="panel"><header className="panel-header"><h3>Equipe cadastrada</h3></header>{loading?<div className="empty">Carregando equipe…</div>:rows.length?<div className="table-wrap"><table className="table"><thead><tr><th>Nome</th><th>Papel</th><th>Permissões</th><th>Status</th><th>Ação</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{row.full_name}</td><td><span className="badge blue">{roleLabels[row.role]}</span></td><td>{permissionsByRole[row.role].length} seções</td><td><span className={`priority-tag tone-${row.active?'green':'red'}`}><i className="priority-dot"/>{row.active?'Ativo':'Inativo'}</span></td><td><button className="btn" onClick={()=>void toggle(row)}>{row.active?'Desativar':'Ativar'}</button></td></tr>)}</tbody></table></div>:<div className="empty">Nenhum membro cadastrado.</div>}</section></section>}
