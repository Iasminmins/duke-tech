import { FormEvent, useEffect, useState } from 'react';
import { supabase } from '../../lib/supabase';
import { EmptyState } from '../../components/ui/EmptyState';

type Product = { id: string; name: string; brand: string | null; condition: string; sale_price: number; quantity: number; minimum_stock: number; active: boolean };
type MovementType = 'entry' | 'exit' | 'adjustment';

const movementLabels = { entry: 'Entrada', exit: 'Saída', adjustment: 'Ajuste' };
const money = (value: number) => Number(value || 0).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });

export function ProductsPage() {
  const [rows, setRows] = useState<Product[]>([]); const [name, setName] = useState(''); const [price, setPrice] = useState(''); const [quantity, setQuantity] = useState(''); const [minimumStock, setMinimumStock] = useState(''); const [open, setOpen] = useState(false); const [editing, setEditing] = useState<Product | null>(null); const [menuId, setMenuId] = useState(''); const [movementProduct, setMovementProduct] = useState<Product | null>(null); const [movementType, setMovementType] = useState<MovementType>('entry'); const [movementQuantity, setMovementQuantity] = useState(''); const [reason, setReason] = useState(''); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [saving, setSaving] = useState(false);
  async function load() { if (!supabase) return; const { data } = await supabase.from('products').select('id,name,brand,condition,sale_price,quantity,minimum_stock,active').is('archived_at', null).order('created_at', { ascending: false }); setRows((data || []) as Product[]); }
  useEffect(() => { load(); }, []);
  function startNew() { setEditing(null); setName(''); setPrice(''); setQuantity(''); setMinimumStock(''); setError(''); setSuccess(''); setOpen(true); setMenuId(''); }
  function startEdit(product: Product) { setEditing(product); setName(product.name); setPrice(String(product.sale_price)); setQuantity(String(product.quantity)); setMinimumStock(String(product.minimum_stock)); setError(''); setOpen(true); setMenuId(''); }
  async function submit(event: FormEvent) { event.preventDefault(); if (!supabase) { setError('Não foi possível conectar ao catálogo.'); return; } setSaving(true); setError(''); const payload = { name: name.trim(), sale_price: Number(price), quantity: Number(quantity), minimum_stock: Number(minimumStock || 0) }; const result = editing ? await supabase.from('products').update(payload).eq('id', editing.id).select('id,name,brand,condition,sale_price,quantity,minimum_stock,active').single() : await supabase.from('products').insert(payload).select('id,name,brand,condition,sale_price,quantity,minimum_stock,active').single(); if (result.error || !result.data) { setError('Não foi possível salvar o produto. Verifique os dados.'); setSaving(false); return; } setRows(prev => editing ? prev.map(row => row.id === editing.id ? result.data as Product : row) : [result.data as Product, ...prev]); setOpen(false); setEditing(null); setSuccess(editing ? 'Produto atualizado com sucesso.' : 'Produto cadastrado com sucesso.'); setSaving(false); }
  async function deactivate(product: Product) { if (!supabase) return; setMenuId(''); if (product.active && !window.confirm(`Desativar o produto ${product.name}?`)) return; const nextActive = !product.active; const result = await supabase.from('products').update({ active: nextActive }).eq('id', product.id); if (result.error) { setError(`Não foi possível ${nextActive ? 'reativar' : 'desativar'} o produto.`); return; } setRows(prev => prev.map(row => row.id === product.id ? { ...row, active: nextActive } : row)); setSuccess(nextActive ? 'Produto reativado.' : 'Produto desativado.'); }
  async function archive(product: Product) { if (!supabase || !window.confirm(`Excluir ${product.name}?`)) return; setMenuId(''); const result = await supabase.from('products').update({ archived_at: new Date().toISOString() }).eq('id', product.id); if (result.error) { setError('Não foi possível excluir o produto.'); return; } setRows(prev => prev.filter(row => row.id !== product.id)); setSuccess('Produto excluído.'); }
  function openMovement(product: Product) { setMovementProduct(product); setMovementType('entry'); setMovementQuantity(''); setReason(''); setError(''); setMenuId(''); }
  async function saveMovement(event: FormEvent) { event.preventDefault(); if (!supabase || !movementProduct) return; const amount = Number(movementQuantity); if (!amount || amount <= 0 || !reason.trim()) { setError('Informe uma quantidade válida e o motivo da movimentação.'); return; } if (movementType === 'exit' && amount > movementProduct.quantity) { setError('A saída não pode ser maior que o estoque atual.'); return; } setSaving(true); setError(''); const nextQuantity = movementType === 'entry' ? movementProduct.quantity + amount : movementType === 'exit' ? movementProduct.quantity - amount : amount; const movement = await supabase.from('stock_movements').insert({ product_id: movementProduct.id, movement_type: movementType, quantity: amount, reason: reason.trim() }); const product = movement.error ? movement : await supabase.from('products').update({ quantity: nextQuantity }).eq('id', movementProduct.id).select('id,name,brand,condition,sale_price,quantity,minimum_stock,active').single(); if (movement.error || product.error || !product.data) { setError('Não foi possível registrar a movimentação.'); setSaving(false); return; } setRows(prev => prev.map(row => row.id === movementProduct.id ? product.data as Product : row)); setMovementProduct(null); setSuccess(`${movementLabels[movementType]} registrada com sucesso.`); setSaving(false); }
  const status = (product: Product) => !product.active ? ['red', 'Inativo'] : product.quantity === 0 ? ['red', 'Sem estoque'] : product.quantity <= product.minimum_stock ? ['gold', 'Estoque baixo'] : ['green', 'Ativo'];
  return <section className="content"><div className="page-heading"><div><div className="eyebrow">Catálogo interno</div><h2>Produtos</h2><p>Cadastre peças, acessórios e serviços para controlar preços e estoque.</p></div><button className="btn primary" onClick={startNew}><span aria-hidden="true">+</span> Produto</button></div>{success && <div className="success" role="status">{success}</div>}{error && !open && !movementProduct && <div className="error" role="alert">{error}</div>}{open && <form className="form-card" onSubmit={submit}><h3>{editing ? 'Editar produto' : 'Novo produto'}</h3><div className="form-grid"><div className="field"><label htmlFor="product-name">Nome <span className="required">*</span></label><input id="product-name" className="input" value={name} onChange={e => setName(e.target.value)} required /></div><div className="field"><label htmlFor="product-price">Preço de venda <span className="required">*</span></label><input id="product-price" className="input" type="number" min="0" step="0.01" value={price} onChange={e => setPrice(e.target.value)} required /></div><div className="field"><label htmlFor="product-quantity">Quantidade <span className="required">*</span></label><input id="product-quantity" className="input" type="number" min="0" value={quantity} onChange={e => setQuantity(e.target.value)} required /></div><div className="field"><label htmlFor="product-minimum">Estoque mínimo</label><input id="product-minimum" className="input" type="number" min="0" value={minimumStock} onChange={e => setMinimumStock(e.target.value)} /></div></div>{error && <div className="error" role="alert">{error}</div>}<div className="actions"><button type="button" className="btn" onClick={() => setOpen(false)}>Cancelar</button><button className="btn primary" disabled={saving}>{saving ? 'Salvando…' : 'Salvar produto'}</button></div></form>}{movementProduct && <form className="form-card" onSubmit={saveMovement}><h3>Ajustar estoque · {movementProduct.name}</h3><div className="form-grid"><div className="field"><label htmlFor="movement-type">Tipo <span className="required">*</span></label><select id="movement-type" className="select" value={movementType} onChange={e => setMovementType(e.target.value as MovementType)}><option value="entry">Entrada</option><option value="exit">Saída</option><option value="adjustment">Ajuste</option></select></div><div className="field"><label htmlFor="movement-quantity">Quantidade <span className="required">*</span></label><input id="movement-quantity" className="input" type="number" min="1" value={movementQuantity} onChange={e => setMovementQuantity(e.target.value)} required /></div><div className="field full"><label htmlFor="movement-reason">Motivo <span className="required">*</span></label><input id="movement-reason" className="input" value={reason} onChange={e => setReason(e.target.value)} placeholder="Ex.: compra do fornecedor" required /></div></div><p className="muted">Estoque atual: {movementProduct.quantity} unidades.</p>{error && <div className="error" role="alert">{error}</div>}<div className="actions"><button type="button" className="btn" onClick={() => setMovementProduct(null)}>Cancelar</button><button className="btn primary" disabled={saving}>{saving ? 'Salvando…' : 'Registrar movimentação'}</button></div></form>}{rows.length === 0 && !open ? <section className="panel"><EmptyState icon="box" title="Nenhum produto cadastrado" description="Cadastre peças, acessórios e serviços para controlar preços e estoque." actionLabel="Cadastrar primeiro produto" actionTo="/admin/produtos" example="Depois você verá quantidade disponível, estoque mínimo e alertas." /></section> : <div className="table-wrap"><table className="table"><thead><tr><th>Produto</th><th>Preço</th><th>Quantidade</th><th>Status</th><th>Ações</th></tr></thead><tbody>{rows.map(row => { const [tone, label] = status(row); return <tr key={row.id}><td><strong>{row.name}</strong><br /><span className="muted">{row.brand || 'Sem marca'}</span></td><td>{money(row.sale_price)}</td><td>{row.quantity}</td><td><span className={`badge ${tone}`}>{label}</span></td><td className="action-cell"><button className="icon-button" aria-label={`Ações para ${row.name}`} aria-expanded={menuId === row.id} onClick={() => setMenuId(menuId === row.id ? '' : row.id)}>•••</button>{menuId === row.id && <div className="row-menu" role="menu"><button onClick={() => startEdit(row)}>Editar produto</button><button onClick={() => openMovement(row)}>Ajustar estoque</button><button onClick={() => deactivate(row)} disabled={!row.active}>Desativar produto</button><button className="danger" onClick={() => archive(row)}>Excluir produto</button></div>}</td></tr>; })}</tbody></table></div>}</section>;
}
