import { FormEvent, useEffect, useState } from 'react';
import { supabase } from '../../lib/supabase';

type Entry = { id: string; kind: 'Entrada' | 'Saída'; description: string; category: string; amount: number; date: string };
const money = (value: number) => Number(value || 0).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });

export function FinancePage() { const [rows, setRows] = useState<Entry[]>([]); const [open, setOpen] = useState(false); const [kind, setKind] = useState<'entry' | 'exit'>('exit'); const [category, setCategory] = useState('Operação'); const [description, setDescription] = useState(''); const [amount, setAmount] = useState(''); const [dueDate, setDueDate] = useState(''); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [saving, setSaving] = useState(false);
  useEffect(() => { if (!supabase) return; Promise.all([supabase.from('expenses').select('id,category,description,amount,due_date,created_at').order('created_at', { ascending: false }).limit(30), supabase.from('payments').select('id,sale_id,amount,paid_at,sales(number)').order('paid_at', { ascending: false }).limit(30)]).then(([expenses, payments]) => { const income = ((payments.data || []) as any[]).map(row => ({ id: row.id, kind: 'Entrada' as const, description: row.sale_id ? 'Venda #' + (Array.isArray(row.sales) ? row.sales[0]?.number || '—' : row.sales?.number || '—') : 'Pagamento recebido', category: 'Vendas', amount: row.amount, date: row.paid_at })); const outgoing = ((expenses.data || []) as any[]).map(row => ({ id: row.id, kind: 'Saída' as const, description: row.description, category: row.category, amount: row.amount, date: row.due_date || row.created_at })); setRows([...income, ...outgoing]); }); }, []);
  async function submit(event: FormEvent) { event.preventDefault(); if (!supabase || !description.trim() || Number(amount) <= 0) { setError('Informe descrição e um valor maior que zero.'); return; } setSaving(true); setError(''); const result = kind === 'exit' ? await supabase.from('expenses').insert({ category, description: description.trim(), amount: Number(amount), due_date: dueDate || null }).select('id,category,description,amount,due_date,created_at').single() : await supabase.from('payments').insert({ amount: Number(amount), payment_method: 'other' }).select('id,amount,paid_at').single(); if (result.error || !result.data) { setError('Não foi possível salvar a movimentação.'); setSaving(false); return; } const row: Entry = kind === 'exit' ? { id: (result.data as any).id, kind: 'Saída', description, category, amount: Number(amount), date: dueDate || new Date().toISOString() } : { id: (result.data as any).id, kind: 'Entrada', description, category, amount: Number(amount), date: (result.data as any).paid_at }; setRows(prev => [row, ...prev]); setDescription(''); setAmount(''); setDueDate(''); setOpen(false); setSuccess('Movimentação registrada com sucesso.'); setSaving(false); }
  const totalIn = rows.filter(row => row.kind === 'Entrada').reduce((sum, row) => sum + Number(row.amount), 0); const totalOut = rows.filter(row => row.kind === 'Saída').reduce((sum, row) => sum + Number(row.amount), 0);
  return <section className="content"><div className="page-toolbar-top"><span className="page-toolbar-count">{`${rows.length} ${rows.length === 1 ? 'movimentação' : 'movimentações'}`}</span><button className="btn primary" onClick={() => { setOpen(value => !value); setError(''); }}>+ Adicionar movimentação</button></div>{success && <div className="success" role="status">{success}</div>}<div className="finance-overview"><article className={`card module-stat is-balance${totalIn - totalOut < 0 ? ' is-negative' : ''}`}><span>Saldo movimentado</span><strong>{money(totalIn - totalOut)}</strong><small>Entradas menos saídas</small></article><article className="card module-stat"><span>Entradas registradas</span><strong>{money(totalIn)}</strong><small>Pagamentos recebidos</small></article><article className="card module-stat"><span>Saídas registradas</span><strong>{money(totalOut)}</strong><small>Despesas lançadas</small></article></div>{open && <form className="form-card" onSubmit={submit}><h3>Nova movimentação</h3><div className="form-grid"><div className="field"><label htmlFor="finance-kind">Tipo <span className="required">*</span></label><select id="finance-kind" className="select" value={kind} onChange={e => setKind(e.target.value as 'entry' | 'exit')}><option value="entry">Entrada</option><option value="exit">Saída</option></select></div><div className="field"><label htmlFor="finance-category">Categoria <span className="required">*</span></label><input id="finance-category" className="input" value={category} onChange={e => setCategory(e.target.value)} required /></div><div className="field"><label htmlFor="finance-description">Descrição <span className="required">*</span></label><input id="finance-description" className="input" value={description} onChange={e => setDescription(e.target.value)} required /></div><div className="field"><label htmlFor="finance-amount">Valor <span className="required">*</span></label><input id="finance-amount" className="input" type="number" min="0.01" step="0.01" value={amount} onChange={e => setAmount(e.target.value)} required /></div><div className="field"><label htmlFor="finance-date">Data</label><input id="finance-date" className="input" type="date" value={dueDate} onChange={e => setDueDate(e.target.value)} /></div></div>{error && <div className="error" role="alert">{error}</div>}<div className="actions"><button type="button" className="btn" onClick={() => setOpen(false)}>Cancelar</button><button className="btn primary" disabled={saving}>{saving ? 'Salvando…' : 'Salvar movimentação'}</button></div></form>}<section className="panel"><header className="panel-header"><h3>Entradas e saídas recentes</h3></header>{rows.length ? <div className="table-wrap"><table className="table"><thead><tr><th>Tipo</th><th>Descrição</th><th>Categoria</th><th>Valor</th><th>Data</th></tr></thead><tbody>{rows.map(row => <tr key={row.id}><td><span className={`badge ${row.kind === 'Entrada' ? 'green' : 'red'}`}>{row.kind}</span></td><td>{row.description}</td><td>{row.category}</td><td>{money(row.amount)}</td><td>{new Date(row.date).toLocaleDateString('pt-BR')}</td></tr>)}</tbody></table></div> : <div className="empty">Nenhuma movimentação registrada.</div>}</section></section>; }
